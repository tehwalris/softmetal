storage:
  files:
    - path: /opt/agent
      filesystem: root
      contents:
        remote:
          url: http://{{softmetalHostPort}}/agent-linux-amd64
      mode: 0777
    - path: /home/core/.bashrc
      filesystem: root
      contents:
        inline: "[[ $- != *i* ]] || sudo /opt/agent"
      mode: 0644
      user:
        name: core
      group:
        name: core
